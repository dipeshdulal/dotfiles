# Credentials — never commit
pi/agent/auth.json
**/auth.json
*.pem
*.key

# Machine-local pi state
pi/agent/sessions/
pi/agent/npm/node_modules/
pi/agent/models-store.json
pi/agent/models.json
pi/agent/git/

# Skill symlinks into a version-pinned global node_modules path
claude/skills/hunk-review

# herdr runtime state
herdr/*.log
herdr/*.sock
herdr/session.json
herdr/release-notes.json
herdr/.plugins.lock

# Backups install.sh makes
*.bak
*.pre-omlx

# Orphaned SST build state (no sst.config.* lives in this repo)
.sst/

.DS_Store
